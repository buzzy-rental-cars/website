<% if user_signed_in? %>
  <nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
      <div class="navbar-header">
        <span class="navbar-brand">Logged in as <%= link_to_twitter current_user.name %>.
          You <%= current_user.administrator? ? "are" : "are not" %> an administrator.</span>
      </div>
      <ul class="nav navbar-nav">
        <li><%= link_to "Log Out", destroy_user_session_path %></li>
        <% if current_user.administrator? %>
          <li><%= link_to "Home Page", root_url %></li>
          <li><%= link_to "Pages", pages_path %></li>
          <li><%= link_to "Users", users_path %></li>
          <li><%= link_to "Vehicles", vehicles_path %></li>
          <li><%= link_to "Vehicle Types", vehicle_types_path %></li>
        <% end %>
      </ul>
    </div>
  </nav>
<% end %>
<%= link_to((image_tag "buzzy-rental-cars-logo.png", alt: "Buzzy Rental Cars Logo", width: 400, height: 175), root_url) %>
